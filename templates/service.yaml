apiVersion: apps/v1
kind: Service

metadata:
    name: nginx

spec:
    type: LoadBalancer
    selector:
        app: nginx

    ports:
    - protocol: tcp
      port: 80
      targetPort: 80        
      nodePort: {{ .Values.service.nodePort }}